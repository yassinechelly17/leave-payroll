export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 720 }}>
      <h1>LeavePayroll</h1>
      <p>Admin UI scaffold — replace this page with your service UI.</p>
      <p>
        Backend health: <code>/api/health</code> (proxied to Spring Boot)
      </p>
    </main>
  );
}
